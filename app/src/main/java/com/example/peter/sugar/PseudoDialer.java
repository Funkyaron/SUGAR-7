package com.example.peter.sugar;

import android.app.Activity;

/**
 * Created by Funkyaron on 11.04.2017.
 *
 * Empty Activity. The app needs to provide a "dialer" (see Manifest file) to make Android accept
 * it as default phone app. This is needed to make Android bind to this app's InCallService.
 * In fact, this activity itself is never used and never needed.
 */

public class PseudoDialer extends Activity {
}
